command /inventory-test:
    trigger:
        inv(player)

function inv(p: player):
    set {_inv} to chest inventory with 1 row 
    set slot integers between 0 and 8 to dirt

    open {_inv} to {_p}
